# 2026-09-08

## 个人简历网站 D:\resume 启动
- 全屏 WELCOME 欢迎页 → 滚动驱动渐变过渡 → 正文（关于/技能/时间线/作品/联系）
- 技术栈：纯静态 HTML + CSS + JS（无构建），单页 index.html，assets/css + assets/js
- 视觉：暗色极光背景 + 星点 canvas + WELCOME 逐字进场 + 滚动模糊淡出放大 + 顶部 transparent→#07080d 渐变承接
- 互动：滚动进度驱动 .welcome 的 --p (淡出/模糊/放大)、.wipe 的 --w (过渡光晕)；enter 按钮 / 进度条 / 进场动画 / 计数 / 文字乱码 / 鼠标光晕 + 极光视差
- 已在本地 127.0.0.1:8000 跑通，截图 7 张（桌面 5 帧 + 移动 2 帧）已确认效果
- GitHub 仓库 https://github.com/Ayfwq/resume 已存在（仅含空 # resume），需 PAT 才能 push
- 部署脚本：.preview/deploy.py 准备就绪（git clone → 写 nginx → certbot → 验证）

## 服务器情况（root@8.138.47.45 / Ubuntu 22.04）
- nginx 1.18 + certbot 1.21 + git 2.34 已装；当前 /var/www/html 是默认欢迎页
- 域名 hellowq.icu 当前 DNS 已设但走到的不是本机：HTTP 502 + "upstream connect failed: 不知道这样的主机 (os error 11001)" → 域名前有反代/CDN，但 CDN 找不到上游 origin
- 推测用户使用了某个 CDN/反代服务（宝塔默认不会这样），需要把 CDN 的 origin 指向 https://8.138.47.45（或裸 IP）才能让 hellowq.icu 真正工作
- 服务器侧可直接 IP 访问，部署后 https://8.138.47.45 会立即可用；等 CDN origin 修好 hellowq.icu 自动恢复

## 阻塞
- 等用户提供 GitHub PAT 才能推送到 Ayfwq/resume
